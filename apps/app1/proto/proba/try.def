
  needs_login
  value_re     .*

# deny exec  : guests
# allow exec : all
# allow exec  read  insert : oper  asd : qwe  !asd : admin
# allow exec  read  insert : oper + asd : qwe + !asd : admin

# allow exec  read  insert : oper  asd : qwe  !asd : admin

# allow exec, read, write to oper + admin, users
# allow exec, read, write : oper + admin : users

@isa base ow

=ID

  value_re  \d+

=DES


=FIELDS_

  name_re   F:[A-Z_0-9]+
  value_re  .*
