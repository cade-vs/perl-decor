
-- record parallel update guard

-- static data rebuild tool

-- avoid INTERNAL ERROR in web when all table fields are forbidden and is empty list

-- return uniq references for core app server errors

-- return "<#e_something>" with arguments!

-- "share" common directory

-- session expire time

-- session atime delay

-- table/classes types

-- table/classes file def mtime to automate dev on-demand alter

-- alter should be a function()

-- debug stack trace of calls sent over the comm between core/web/etc.

-- debug stack on low-level calls (DB::IO)

-- avoid DateTime.pm and all the mess

-- docs for custom server modules! app1 [Decor::Core::Net::Server::Echo]